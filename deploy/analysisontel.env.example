# Web dashboard
WEB_HOST=0.0.0.0
WEB_PORT=9443
WEB_PASSWORD=change_me_strong_password

# Auto-refresh interval (minutes) — 240 = every 4 hours
REFRESH_INTERVAL_MINUTES=240

# Risk limits
MAX_RISK_PCT=1.0
MAX_LEVERAGE=20
